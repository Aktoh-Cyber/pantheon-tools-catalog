# vuln-match

Synapse v2 tool: match an installed-package list against an advisory feed (Tier 0, pure compute). judge's flagship gap.

Synapse v2 node tool. JSON args on argv, one JSON line on stdout. Exit 0 evaluated, 1 arg error.
