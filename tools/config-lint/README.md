# config-lint

Synapse v2 tool: lint a security config (sshd_config) against a hardening baseline (Tier 0, pure compute).

Synapse v2 node tool. JSON args on argv, one JSON line on stdout. Exit 0 evaluated, 1 arg error.
